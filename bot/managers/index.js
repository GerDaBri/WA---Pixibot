/**
 * Exportaciones de managers
 */

const ClientManager = require('./client-manager');
const SessionManager = require('./session-manager');

module.exports = {
    ClientManager,
    SessionManager
};
